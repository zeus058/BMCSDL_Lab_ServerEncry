<UserControl x:Class="StudentManager.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:viewmodels="clr-namespace:StudentManager.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:DashboardViewModel}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="4">
            <TextBlock Text="Tổng quan công việc" Style="{StaticResource PageTitle}" />
            <TextBlock Text="Dữ liệu trên màn hình này được tính trong phạm vi các lớp do nhân viên hiện tại phụ trách."
                       Foreground="{StaticResource TextMuted}"
                       Margin="0,0,0,18" />

            <UniformGrid Columns="4" Margin="0,0,0,18">
                <Border Style="{StaticResource CardBorder}" Margin="0,0,10,10">
                    <StackPanel>
                        <TextBlock Text="Lớp phụ trách" Foreground="{StaticResource TextMuted}" />
                        <TextBlock Text="{Binding ManagedClasses}" FontSize="34" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" Margin="0,10,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardBorder}" Margin="10,0,10,10">
                    <StackPanel>
                        <TextBlock Text="Sinh viên quản lý" Foreground="{StaticResource TextMuted}" />
                        <TextBlock Text="{Binding ManagedStudents}" FontSize="34" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" Margin="0,10,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardBorder}" Margin="10,0,10,10">
                    <StackPanel>
                        <TextBlock Text="Học phần" Foreground="{StaticResource TextMuted}" />
                        <TextBlock Text="{Binding TotalCourses}" FontSize="34" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" Margin="0,10,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardBorder}" Margin="10,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Bản ghi điểm" Foreground="{StaticResource TextMuted}" />
                        <TextBlock Text="{Binding EnteredGrades}" FontSize="34" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" Margin="0,10,0,0" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.35*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardBorder}" MinHeight="360">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="Số lượng sinh viên theo lớp" Style="{StaticResource SectionTitle}" Margin="0,0,0,12" />
                        <lvc:CartesianChart Series="{Binding StudentCountSeries}" LegendLocation="None">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Labels="{Binding Labels}">
                                    <lvc:Axis.Separator>
                                        <lvc:Separator Step="1" IsEnabled="False" />
                                    </lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis LabelFormatter="{Binding Formatter}" MinValue="0" />
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </DockPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource CardBorder}" MinHeight="360">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="Tình trạng nhập điểm" Style="{StaticResource SectionTitle}" Margin="0,0,0,12" />
                        <TextBlock DockPanel.Dock="Top"
                                   Text="Tỷ lệ sinh viên đã có ít nhất một dòng điểm trong phạm vi lớp bạn quản lý."
                                   Foreground="{StaticResource TextMuted}"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,12" />
                        <lvc:PieChart Series="{Binding CoverageSeries}" LegendLocation="Bottom" />
                    </DockPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
